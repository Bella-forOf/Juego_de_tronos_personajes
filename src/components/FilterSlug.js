const FilterSlug = () => {

    return (
        <input
            className="header__search"
            autoComplete="off"
            type="search"
            name="search"
            placeholder="Filtrar por Mote"
            // onInput={handleNew}
            // value={search}
          />
    );
  };
  
  export default FilterSlug;
        